import {sma,ema,rsi,macd,atr,obv,roc,adx,supertrend} from './indicators.js';
const clamp=x=>Math.max(0,Math.min(100,Math.round(x||0))); const last=a=>{for(let i=a.length-1;i>=0;i--)if(Number.isFinite(a[i]))return a[i];return null};
export function analyze(rows){if(!Array.isArray(rows)||rows.length<60)throw new Error('Yeterli veri yok');const c=rows.map(x=>x.close),h=rows.map(x=>x.high),l=rows.map(x=>x.low),v=rows.map(x=>x.volume||0),n=c.length-1;const e20=ema(c,20),e50=ema(c,50),e200=ema(c,200),ma20=sma(c,20),ma50=sma(c,50),rs=rsi(c),mc=macd(c),at=atr(h,l,c),ro=roc(c),ob=obv(c,v),vol=sma(v,20),di=adx(h,l,c),sup=supertrend(h,l,c);const close=c[n],prev=c[n-1]||close,change=((close-prev)/prev)*100,atrPct=((at[n]||0)/close)*100;let trend=0,money=0,momentum=0,risk=10,pattern=35;if(close>e20[n])trend+=15;if(close>e50[n])trend+=25;if(close>e200[n])trend+=25;if(e50[n]>e200[n])trend+=15;if((di.adx[n]||0)>25&&di.pdi[n]>di.mdi[n])trend+=20;if(sup.dir[n]===1)trend+=15;if(v[n]>(vol[n]||1)*1.3)money+=35;if(ob[n]>(ob[Math.max(0,n-20)]||0))money+=25;if(v[n]>(vol[n]||1))money+=20;if(close>c[Math.max(0,n-10)])money+=20;if(rs[n]>45&&rs[n]<65)momentum+=25;if(mc.m[n]>mc.s[n])momentum+=30;if((mc.h[n]||0)>(mc.h[n-1]||0))momentum+=20;if((ro[n]||0)>0)momentum+=25;if(atrPct>6)risk+=35;else if(atrPct>4)risk+=20;else if(atrPct>2.5)risk+=10;if(rs[n]>75)risk+=20;if(Math.abs(change)>7)risk+=20;const lows=c.slice(Math.max(0,n-70));const min=Math.min(...lows),max=Math.max(...lows);if((close-min)/(max-min||1)<.35 && mc.m[n]>mc.s[n])pattern=65;if(close>ma20[n]&&ma20[n]>ma50[n])pattern=75;trend=clamp(trend);money=clamp(money);momentum=clamp(momentum);risk=clamp(risk);const potential=clamp(trend*.30+money*.25+momentum*.25+pattern*.20);const confidence=clamp(trend*.30+money*.20+momentum*.20+pattern*.15+(100-risk)*.15);const finalScore=clamp(confidence*.45+potential*.35+(100-risk)*.20);const decision=finalScore>=80?'GÜÇLÜ AL':finalScore>=65?'AL':finalScore>=50?'İZLE':finalScore>=35?'BEKLE':'RİSKLİ';const target1=close*(1+Math.max(5,potential/7)/100),target2=close*(1+Math.max(10,potential/4)/100),stop=close*(1-(4+risk/18)/100);const positives=[],negatives=[];const add=(ok,t)=>ok?positives.push(t):negatives.push(t);add(close>e50[n],'EMA50 üstünde');add(close>e200[n],'EMA200 üstünde');add(mc.m[n]>mc.s[n],'MACD pozitif');add((di.adx[n]||0)>25,'ADX trend güçlü');add(sup.dir[n]===1,'SuperTrend AL');add(v[n]>(vol[n]||1),'Hacim ortalama üstü');add(risk<50,'Risk kabul edilebilir');return{date:rows[n].date,close,prev,change,volume:v[n],volAvg:vol[n]||0,ema20:e20[n],ema50:e50[n],ema200:e200[n],sma20:ma20[n],sma50:ma50[n],rsi:rs[n],macd:mc.m[n],signal:mc.s[n],hist:mc.h[n],atr:at[n],atrPct,roc:ro[n],obv:ob[n],adx:di.adx[n],pdi:di.pdi[n],mdi:di.mdi[n],superTrend:sup.st[n],superTrendDir:sup.dir[n],trend,money,momentum,pattern,risk,potential,confidence,finalScore,decision,target1,target2,stop,formation:{name:pattern>70?'Trend devam formasyonu':pattern>60?'Dipten dönüş adayı':'Belirgin formasyon yok',score:pattern},positives,negatives,ohlcv:rows}}
export function comments(symbol,a){return{expert:`${symbol}: Trend ${a.trend}/100, para girişi ${a.money}/100, momentum ${a.momentum}/100. ADX ${Number(a.adx||0).toFixed(1)}, SuperTrend ${a.superTrendDir===1?'AL':'SAT'}.`,ai:`Karar motoru ${symbol} için ${a.decision} sonucunu üretiyor. Güven %${a.confidence}, risk %${a.risk}. Stop ve hedef seviyeleri ile takip edilmelidir.`}}
