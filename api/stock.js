import {getOhlcv} from '../lib/provider.js';import {analyze,comments} from '../lib/engine.js';
export default async function handler(req,res){try{const symbol=String(req.query.symbol||'PAPIL').toUpperCase();const rows=await getOhlcv(symbol,'1y','1d');const analysis=analyze(rows);res.status(200).json({success:true,symbol,analysis,comments:comments(symbol,analysis),ohlcv:rows})}catch(e){res.status(200).json({success:false,error:e.message})}}
