import { getSymbols, analyzeDipPro } from '../lib/engine.js';
export default async function handler(req,res){const offset=Math.max(0,Number(req.query.offset||0));const limit=Math.min(20,Math.max(1,Number(req.query.limit||10)));const list=getSymbols();const data=list.slice(offset,offset+limit).map(analyzeDipPro);res.status(200).json({success:true,total:list.length,offset,limit,count:data.length,data});}
